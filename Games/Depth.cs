using System.Linq;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Flash : MonoBehaviour {

	void Start()
	{
		//Target.gameObject.transform.localScale += Vector3 (10.1f, 0, 0);
	}

	void Update()
	{
		//gameObject.transform.localScale = Vector3 (10.1, 0, 0);
	}
}